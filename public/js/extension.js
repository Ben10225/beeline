// let messageSubmit = async (e, socket) => {
//     e.preventDefault();
//     let input = document.querySelector("#send-message");
//     let message = input.value;
//     input.value = "";
//     socket.emit('chat', ROOM_ID, USER_NAME, message);
// }

export default {
    messageSubmit
}